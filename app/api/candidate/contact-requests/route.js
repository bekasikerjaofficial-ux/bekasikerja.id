import { NextResponse } from 'next/server';
import { requireUser, badRequest } from '../../../../lib/employer-auth';
export async function GET(request){const auth=await requireUser(request);if(auth.error)return auth.error;const {data,error}=await auth.db.from('employer_contact_requests').select('*, companies(name)').eq('candidate_id',auth.user.id).order('created_at',{ascending:false});if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({requests:data||[]});}
export async function PATCH(request){const auth=await requireUser(request);if(auth.error)return auth.error;const body=await request.json();if(!['accepted','rejected'].includes(body.status)||!body.id)return badRequest('Permintaan tidak valid.');const {data,error}=await auth.db.from('employer_contact_requests').update({status:body.status,responded_at:new Date().toISOString()}).eq('id',body.id).eq('candidate_id',auth.user.id).select('*').single();if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({request:data});}
