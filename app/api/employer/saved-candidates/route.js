import { NextResponse } from 'next/server';
import { requireEmployer, badRequest } from '../../../../lib/employer-auth';

export async function GET(request) { const auth = await requireEmployer(request); if (auth.error) return auth.error; const { data, error } = await auth.db.from('saved_candidates').select('*, candidate_profiles(*)').eq('company_id', auth.company.id).order('created_at', { ascending: false }); if (error) return NextResponse.json({ error: error.message }, { status: 400 }); return NextResponse.json({ candidates: data || [] }); }
export async function POST(request) { const auth = await requireEmployer(request); if (auth.error) return auth.error; const body = await request.json(); if (!body.candidateId) return badRequest('candidateId wajib.'); const { data, error } = await auth.db.from('saved_candidates').insert({ company_id: auth.company.id, candidate_id: body.candidateId, note: body.note || null }).select('*').single(); if (error) return NextResponse.json({ error: error.code === '23505' ? 'Kandidat sudah disimpan.' : error.message }, { status: 400 }); return NextResponse.json({ saved: data }, { status: 201 }); }
export async function DELETE(request) { const auth = await requireEmployer(request); if (auth.error) return auth.error; const candidateId = new URL(request.url).searchParams.get('candidateId'); if (!candidateId) return badRequest('candidateId wajib.'); const { error } = await auth.db.from('saved_candidates').delete().eq('company_id', auth.company.id).eq('candidate_id', candidateId); if (error) return NextResponse.json({ error: error.message }, { status: 400 }); return NextResponse.json({ success: true }); }
